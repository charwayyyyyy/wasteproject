import { CollectorLayout } from "@/components/layout/collector-layout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <CollectorLayout>{children}</CollectorLayout>;
}
